services:
  content-agent:
    build: .
    container_name: content_ideas_agent
    env_file: .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./logs:/app/logs
    restart: unless-stopped
