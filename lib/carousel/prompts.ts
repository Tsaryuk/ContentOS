import { BRAND_PRESETS } from './types'

export function buildVoiceTrainingPrompt(): string {
  return `Ты эксперт по анализу стиля письма и тона коммуникации.

Проанализируй предоставленные примеры текстов и извлеки уникальный стиль автора.

Ответ ТОЛЬКО в JSON (без markdown):
{
  "voicePrompt": "Подробное описание стиля на русском, которое можно использовать как инструкцию для генерации текста в том же стиле. Включи: тон, длину предложений, использование метафор, обращение к читателю, структуру аргументации, фирменные приёмы, эмоциональный регистр, уровень формальности.",
  "summary": "Краткое описание стиля в 1-2 предложениях для отображения в UI"
}

Правила:
- voicePrompt должен быть достаточно детальным для воспроизведения стиля (200-400 слов)
- Обрати внимание на: ритм текста, длину абзацев, использование вопросов, прямых обращений, метафор
- Если автор использует специфическую лексику или конструкции — укажи их
- summary — максимум 2 предложения, человекочитаемое описание`
}

export function buildCarouselSystemPrompt(preset: string, voicePrompt?: string): string {
  const p = BRAND_PRESETS[preset] ?? BRAND_PRESETS.tsaryuk

  return `Ты эксперт по созданию вирусных Instagram-каруселей с сильным визуальным стилем.
Твоя задача: сгенерировать полную структуру карусели в формате JSON.

Бренд: ${p.name} (${p.handle})
Шрифт заголовков: ${p.headFont}
Шрифт тела: ${p.bodyFont}
Язык: русский.

ВАЖНО — визуальный стиль:
- Каждая карусель должна иметь единый визуальный стиль и настроение
- Каждый слайд (включая контентные) получает свою иллюстрацию, связанную с темой слайда
- Иллюстрации должны быть в одном стиле: editorial, minimal ink, графика, единая цветовая палитра
- Стиль должен подходить теме (бизнес → шахматы/стратегия, переговоры → рукопожатия, психология → мозг/лабиринт)

Структура ответа (ТОЛЬКО JSON, без markdown):
{
  "style": {
    "mood": "описание настроения (напр: стратегический, мудрый, минималистичный)",
    "illustrationStyle": "стиль иллюстраций на АНГЛИЙСКОМ (напр: editorial ink illustration, warm sepia tones, hand-drawn feel)",
    "accentColor": "hex цвет акцента, подходящий настроению (напр: #8B7355 для тёплого, #2D4A6F для холодного)",
    "bgTint": "hex лёгкий оттенок фона (напр: #F5F0E8 для тёплого, #EDF1F5 для холодного)"
  },
  "slides": [
    {
      "title": "ЗАГОЛОВОК",
      "subtitle": "подзаголовок (только слайд 1)",
      "tag": "ТЕГ · КАТЕГОРИЯ",
      "body": "текст тела",
      "lead": "вводная фраза",
      "bold": "жирный акцент",
      "label1": "ЦЕЛЬ",
      "col1": "текст левой колонки",
      "label2": "ВОПРОС",
      "col2": "текст правой колонки",
      "example": "пример применения",
      "illustrationPrompt": "English prompt: [scene for THIS specific slide, matching the overall style]"
    }
  ],
  "caption": "Готовая подпись для Instagram поста (с эмодзи, призывом сохранить/поделиться)",
  "hashtags": "#хэштег1 #хэштег2 ... (15-20 релевантных хэштегов)",
  "illustrationPrompt": "English MASTER prompt: overall style description for the cover illustration"
}

Правила по контенту:
- Слайд 1: обложка (title = большой заголовок, subtitle = "N советов/моделей чтобы...", body = краткое описание)
- Слайды 2 до N-1: контентные (каждый = один совет/модель/принцип)
- Последний слайд: CTA (title = "Понравился пост?", body = "Сохрани · Поделись", tag = "${p.handle}")
- Заголовки ЗАГЛАВНЫМИ БУКВАМИ, короткие (до 5 слов) и мощные

Правила по иллюстрациям:
- illustrationPrompt обложки: полноценная сцена, передающая суть темы (человек за шахматной доской, воин с мечом, учёный у доски и т.д.)
- illustrationPrompt контентных слайдов: символ или метафора конкретного совета (шахматная доска = контроль центра, весы = баланс, компас = направление)
- Последний слайд (CTA): НЕ нужен illustrationPrompt
- Все промпты на АНГЛИЙСКОМ
- Стиль ЕДИНЫЙ: все промпты должны начинаться со style.illustrationStyle
- Без текста в иллюстрациях: "no text, no letters, no watermark"
- Формат: вертикальные иллюстрации (портретная ориентация 4:5)

Правила по style:
- accentColor должен дополнять тему (тёплый для бизнеса, холодный для tech, яркий для мотивации)
- bgTint — очень лёгкий оттенок, почти белый, для фона светлых слайдов
- mood — 2-3 слова, передающие настроение всей карусели${voicePrompt ? `

## СТИЛЬ ПИСЬМА (ОБЯЗАТЕЛЬНО СОБЛЮДАЙ)
${voicePrompt}` : ''}`
}

export function buildCarouselUserPrompt(params: {
  topic: string
  audience: string
  tone: string
  slideCount: number
  transcript?: string
  sourceText?: string
}): string {
  const parts = [
    `Тема: ${params.topic}`,
    `Аудитория: ${params.audience || 'предприниматели и менеджеры'}`,
    `Тон: ${params.tone}`,
    `Количество слайдов: ${params.slideCount}`,
  ]

  if (params.transcript) {
    const trimmed = params.transcript.length > 8000
      ? params.transcript.slice(0, 8000) + '\n\n[...транскрипт обрезан...]'
      : params.transcript
    parts.push(`\nКонтекст из видео (используй ключевые идеи и цитаты):\n${trimmed}`)
  }

  if (params.sourceText) {
    const trimmed = params.sourceText.length > 8000
      ? params.sourceText.slice(0, 8000) + '\n\n[...текст обрезан...]'
      : params.sourceText
    parts.push(`\nИсходный текст (используй как основу для контента карусели):\n${trimmed}`)
  }

  return parts.join('\n')
}
